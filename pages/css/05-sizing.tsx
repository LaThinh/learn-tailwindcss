import LayoutCss from "@/components/layout/LayoutCss";
import React from "react";

export default function TailwindSizing() {
  return (
    <div>
      <LayoutCss title="Tailwind Sizing">sizing</LayoutCss>
    </div>
  );
}
